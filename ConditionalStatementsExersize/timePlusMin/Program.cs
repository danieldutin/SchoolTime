using System;

namespace timePlusMin
{
    class Program
    {
        static void Main(string[] args)
        {
            int hour = int.Parse(Console.ReadLine());
            int min = int.Parse(Console.ReadLine());
            string exactHr;
            string exactMin;
            if (min <10)
            {
                exactHr = hour;
                exactMin 
            }
        }
    }
}
